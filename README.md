# repo-frontend

Frontend demo repo for this workspace.

It depends on the sibling `../infra-base` repo for shared compose fragments and env defaults.

Run locally:

```bash
./up.sh dev
```

Security:

- .gitleaks.toml is included for repo-level secret scanning

